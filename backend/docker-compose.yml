version: '3'
services:
  database:
    image: 'postgres:latest'
    ports:
      - '5432:5432'
    environment:
      POSTGRES_USER: 'tourplanner'
      POSTGRES_PASSWORD: 'sekuriti'
      POSTGRES_DB: 'tourplanner'